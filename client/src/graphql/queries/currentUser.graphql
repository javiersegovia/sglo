query currentUser {
  currentUser {
    id
    email
    firstName
    lastName
    verification {
      verifiedEmail
    }
    companyMember {
      roles
      company {
        id
        name
      }
    }
  }
}
